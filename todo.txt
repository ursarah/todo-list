handleClick:
    - Recebe um argumento que se chama id que é o id da task individual
    - Na função ele vai:
        * Criar um novo array usando map
        * Nas condições do map para criar o array ele vai:
            - Pegar task.id e comparar com id que veio como argumento
            - Se eles forem iguais:
                # criar um novo objeto com as propiedades do antigo objeto(usando spread) mudando só o checked para a negativa do antigo
            - se não forem iguais:
                # as proximas arrays que não forem iguais vão continuar a mesma coisa sem mudar o estado

handleTime:
    - Ele vai no atributo onChange para verificar toda vez que o estado do imput muda
    - Primeiro ele muda o estado do setTime para colocar na array de objetos
    - Depois ele cria uma nova variavel para pegar o valor do input
    - Dai ele verifica:
        * Quando o tamanho da variavel for maior que 2:
            - ele faz um slice* nos 2 primeiro digitos coloca ":"
            - Adiciona outro slice do terceiro digito pra frente
    * Slice:
        - ele separa em n digitos em um nova variavel, colocando como argumentos, começando do 0, o inicio e o fim da separação, sendo que o digito do fim não é contado
        - exemplo: 
            let nome = "Sarah"
            nome.slice(0,2) //Res: "Sa"